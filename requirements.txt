# audio-stream-gateway — runtime dependencies.
#
# pyproject.toml is the source of truth; this file is generated from its
# [project] dependencies for tools and reviewers that expect it.
#   pip install -e .          installs exactly this
#   pip install -e ".[dev]"   adds the test dependencies

fastapi>=0.110
uvicorn[standard]>=0.27
